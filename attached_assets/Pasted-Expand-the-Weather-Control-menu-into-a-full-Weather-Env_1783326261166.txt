Expand the Weather Control menu into a full Weather & Environment editor.

UI Improvements:
- Make the menu noticeably wider while keeping approximately the same height.
- Keep the existing design, colors, rounded corners, and styling.
- Organize the controls into sections (Weather, Lighting, Atmosphere, Clouds, Time, etc.).
- Add sliders, toggles, and input boxes where appropriate instead of only buttons.

Weather Controls:
- Continue supporting the existing weather presets.
- Allow live adjustment of each weather instead of using fixed values.
- Examples:
  - Rain intensity
  - Rain speed
  - Fog density
  - Fog end distance
  - Wind strength
  - Snow intensity
  - Sandstorm intensity
  - Thunder frequency
  - Lightning brightness
  - Ambient sound volume
- Changes should apply immediately as the sliders are adjusted.

Lighting Controls:
- Time of day slider (0–24 hours).
- Brightness.
- Exposure compensation.
- Ambient color.
- Outdoor ambient.
- Shadow softness.
- ClockTime.
- Geographic latitude (if supported).

Atmosphere Controls:
- Density.
- Haze.
- Glare.
- Offset.
- Color.
- Decay.

Cloud Controls:
- Cloud cover.
- Cloud density.
- Cloud color.

Environment Controls:
- Enable/disable sun rays.
- Enable/disable bloom.
- Enable/disable color correction.
- Enable/disable depth of field.
- Enable/disable atmosphere.
- Enable/disable clouds.

Additional Features:
- Add a "Reset to Default" button that restores Roblox's default environment.
- Add an "Apply" button if needed for settings that shouldn't update live.
- Continue showing the currently active weather.
- Prevent duplicate weather effects.
- Ensure all changes replicate to every player.
- Players joining while a custom environment is active should immediately receive the current settings.
- Keep everything modular so additional weather types and controls can easily be added later.

Do not rewrite the existing weather system. Extend it into a complete weather and environment editor while preserving all current functionality.
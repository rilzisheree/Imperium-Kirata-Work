Before making any changes, thoroughly read and understand my existing admin command system, character handling, RemoteEvents, and coding style. Reuse the current architecture and only add the functionality described below.

Create two new admin commands:

invis <target>

uninvis <target>

--------------------------------------------------
INVIS
--------------------------------------------------

Description:
- Makes the specified player(s) invisible.

Requirements:
- Follow the existing admin command framework and coding style.
- Support all existing target selectors (username, display name, me, all, team selectors, etc.) if the command system already provides them.
- Make the player's entire character invisible, including body parts, clothing, accessories, layered clothing, hats, and any visible character components.
- Preserve the player's movement, animations, tools, and gameplay functionality.
- The player should remain fully interactive with the game.
- Ensure the invisibility persists until `uninvis` is used or the player respawns (unless the existing system already persists effects).
- Prevent duplicate invisibility effects if the command is run multiple times.

--------------------------------------------------
UNINVIS
--------------------------------------------------

Description:
- Restores visibility to the specified player(s).

Requirements:
- Restore every character component back to its original visibility.
- Correctly restore body parts, clothing, accessories, layered clothing, and any modified transparency values.
- If the player is not invisible, do nothing without throwing errors.
- Handle multiple targeted players correctly.

--------------------------------------------------
GENERAL REQUIREMENTS
--------------------------------------------------

- Keep the implementation efficient and avoid memory leaks.
- Preserve all existing character functionality.
- Handle players who are respawning or whose characters are temporarily unavailable.
- Integrate seamlessly into the existing admin system without breaking any current commands or functionality.
- Follow the existing coding style and project architecture.
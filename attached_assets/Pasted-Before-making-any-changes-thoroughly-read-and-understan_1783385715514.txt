Before making any changes, thoroughly read and understand my existing admin command system, player handling, RemoteEvents, and coding style. Reuse the current architecture and only add the functionality described below.

Create two new admin commands:

to <target>

tp <player1> <player2>

--------------------------------------------------
TO
--------------------------------------------------

Description:
- Teleports the admin who executed the command to the specified player.

Requirements:
- Follow the existing admin command framework and coding style.
- Support all existing target selectors (username, display name, me, all, team selectors, etc.) if the command system already provides them.
- Teleport the admin to a safe position a few studs in front of the target player to prevent overlapping characters.
- Wait until both players' HumanoidRootParts exist before teleporting.
- Ensure the admin is not teleported inside walls, objects, or beneath the map.
- Gracefully handle players who are respawning or whose characters are temporarily unavailable.
- Integrate seamlessly into the existing admin system without breaking any current functionality.

--------------------------------------------------
TP
--------------------------------------------------

Description:
- Teleports Player 1 to Player 2.

Usage:
tp <player1> <player2>

Requirements:
- Follow the existing admin command framework and coding style.
- Support all existing target selectors.
- Teleport Player 1 to a safe position a few studs in front of Player 2.
- Preserve Player 1's character state where possible.
- Wait until both players' HumanoidRootParts exist before teleporting.
- Support multiple targeted players if the existing command framework allows it (e.g. `tp all Kirata`).
- Prevent players from being teleported inside walls, objects, or beneath the map.
- Gracefully handle players who are respawning or whose characters are temporarily unavailable.
- Integrate seamlessly into the existing admin system without breaking any current commands or functionality.

--------------------------------------------------
GENERAL REQUIREMENTS
--------------------------------------------------

- Follow the existing coding style and project architecture.
- Prevent duplicate errors or unnecessary teleport attempts.
- Ensure all teleports are smooth, reliable, and performant.
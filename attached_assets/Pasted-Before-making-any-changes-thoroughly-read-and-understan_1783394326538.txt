Before making any changes, thoroughly read and understand my existing admin command system, player handling, UI, RemoteEvents, and coding style. Reuse the current architecture and only add the functionality described below.

Create a new admin command:

esp <target>

Description:
- Enables ESP for the specified player(s), allowing the admin to see important information about them through walls.

Requirements:
- Follow the existing admin command framework and coding style.
- Support all existing target selectors (username, me, all, team selectors, etc.) if the command system already provides them.
- ESP should only be visible to the admin who executed the command.
- Draw an ESP overlay above each targeted player's character that remains visible through walls.
- The ESP should update live in real time.

Each ESP should display:
- @Username
- Current Health / Maximum Health
- Current Team (using the team's color)
- Current Location (if a location/region system exists; otherwise display the name of the nearest map area or "Unknown")
- Distance from the admin (e.g. 152m)

Visual Requirements:
- Use a clean, modern floating UI above the player's head.
- Include a subtle outline for readability.
- Scale appropriately based on distance so it remains readable.
- Optionally include a highlight/outline around the player's character.

Behavior:
- ESP should continue updating as players move, change teams, take damage, heal, or respawn.
- If a targeted player leaves the game, automatically remove their ESP.
- If the admin dies or respawns, ESP should continue functioning.
- Prevent duplicate ESP overlays if the command is executed multiple times.

Additional Command Behavior:
- Running `esp` again on the same player should toggle their ESP off.
- Running `esp all` should toggle ESP for every player.
- Cleanly remove all UI, highlights, and event connections when ESP is disabled.

General Requirements:
- Keep the implementation optimized for performance.
- Avoid unnecessary loops or memory leaks.
- Integrate seamlessly into the existing admin system without breaking any current functionality.
- Follow the existing coding style and project architecture.